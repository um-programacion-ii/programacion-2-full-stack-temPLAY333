# Configuracion para tests unitarios (sin servicios externos)
spring.application.name=proxy-test

# Deshabilitar Kafka y Redis para tests unitarios
spring.kafka.enabled=false
spring.data.redis.enabled=false
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration,org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

# Usar credenciales reales (para que AuthTokenService no falle en tests)
app.catedra.base-url=${APP_CATEDRA_BASE_URL:http://192.168.194.250:8080}
app.catedra.api-base=/api
app.catedra.username=${APP_CATEDRA_USERNAME:templay333}
app.catedra.password=${APP_CATEDRA_PASSWORD:B0lud0t0t4l}
app.catedra.token-ttl-seconds=3600

# Redis embebido o mock
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Kafka mock (no se usa realmente)
spring.kafka.bootstrap-servers=localhost:9092
app.kafka.consumer.group-id=test-grupo
app.kafka.consumer.topic=test-topic

# Logging m?nimo
logging.level.root=WARN
logging.level.um.prog2=INFO

